<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Renareich - Secret Santa{% endblock %}</title>
        <link rel="stylesheet" href="https://bootswatch.com/4/journal/bootstrap.min.css">
        <base href="/">
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            
            <a class="navbar-brand" href="{{ path('rena_home') }}"><img src="img/renaV2.png" alt="" width="120px" height="120px"></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('instructions') }}">Instructions</a>
                    </li>
                    {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link"  href="{{ path('envoie') }}">Envoyer mon cadeau</a>
                    </li>
                    {% endif %}
                </ul>
                {% if app.user is same as(null) %}
                    <a class="btn btn-primary nav-link mr-sm-2" href="{{ path('inscription') }}">Inscription</a>
                    <a class="btn btn-primary nav-link mr-sm-2" href="{{ path('app_login') }}">Connexion</a>
                {% else %}
                    <div class="nav-link mr-sm-2 text-secondary">
                        <a class ="btn btn-primary" href="{{ path('profile') }}">{{ app.user.username }}</a>
                        <a class ="btn btn-secondary" href="{{ path('app_logout') }}">Logout</a>
                    </div>               
                {% endif %}
            </div>
        </nav>
        <br>
        <div class="container">
            {% block body %}{% endblock %}
        </div>
        {% block javascripts %}{% endblock %}
    </body>
</html>